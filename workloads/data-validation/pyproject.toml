[project]
name = "data-validation"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "boto3==1.34.0",
    "polars==0.20.3",
    "setuptools==81.0.0",
    "python-dotenv==1.2.3",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.uv]
package-mode = false


